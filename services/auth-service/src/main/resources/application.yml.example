server:
  port: 8080

spring:
  application:
    name: auth-service

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://hostname:5432/postgres
    username: username
    password: username

  jpa:
    open-in-view: false